<div class="card shadow-sm p-4">
  <%= simple_form_for [restaurant, review] do |f| %>
    <%= f.input :rating, collection: 0..5, label: "Rating (0–5)" %>
    <%= f.input :content, label: "Your review" %>

    <%= f.submit "Submit review", class: "btn btn-primary mt-3" %>
  <% end %>
</div>
